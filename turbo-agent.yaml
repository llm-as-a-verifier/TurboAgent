backend:
  models:
    - name: gemini/gemini-3-flash-preview
      api_key: $GEMINI_API_KEY
      num_candidates: 9        # parallel requests per model
      temperature: 1
      thinking: high          # none, low, medium, high, or token budget
      max_tokens: 65536

verifier:
  model:
    name: gemini/gemini-2.5-flash
    api_key: $GEMINI_API_KEY
    max_top_logprobs: 20

  method: 
    name: pivot_tournament
    <fill params>